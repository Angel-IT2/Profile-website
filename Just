import React from "react";
import aboutData from "./about.json";
import "./About.css";

const About = () => {
  return (
    <div className="about-container">
      <div className="about-contents">
        {/* Header Section */}
        <header className="about-header">
          <h1>{aboutData.title || "About UniPath"}</h1>
          <p>
            {aboutData.subtitle ||
              "Empowering first-year students to achieve academic wellness and confidence through peer connection and shared learning."}
          </p>
        </header>

        {/* Academic Wellness Section */}
        <section className="wellbeing-sections">
          <h2>Academic Wellness at UniPath</h2>
          <p>
            At UniPath, we believe academic wellness is the foundation of a
            successful university journey. It goes beyond good grades — it’s
            about developing effective study habits, time management, and
            resilience through collaboration and mentorship.
          </p>

          {aboutData.sections &&
            aboutData.sections.map((section, index) => (
              <div key={index} className="wellbeing-section">
                <div className="section-content">
                  <div className="section-text">
                    <h3>{section.heading}</h3>
                    <ul>
                      {section.items.map((item, i) => (
                        <li key={i}>{item}</li>
                      ))}
                    </ul>
                  </div>
                </div>
              </div>
            ))}
        </section>

        {/* Benefits Section */}
        <section className="benefits-section">
          <h2>Benefits of Academic Wellness with UniPath</h2>
          <p>
            UniPath supports students in achieving balance between their
            academic goals and personal growth. Here’s how our platform
            enhances academic wellness:
          </p>

          {aboutData.benefits &&
            Object.entries(aboutData.benefits).map(([key, items], index) => (
              <div key={index} className="benefit-category">
                <h3>{key}</h3>
                <ul>
                  {items.map((item, i) => (
                    <li key={i}>{item}</li>
                  ))}
                </ul>
              </div>
            ))}
        </section>

        {/* Closing Message */}
        <footer className="about-footer">
          <h2>Our Philosophy</h2>
          <p>
            “It’s about learning, not about being the smartest.”  
            UniPath fosters a supportive academic culture where students learn
            from one another, grow together, and thrive in their university
            journey.
          </p>
        </footer>
      </div>
    </div>
  );
};

export default About;
